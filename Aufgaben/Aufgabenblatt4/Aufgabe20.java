package Aufgaben.Aufgabenblatt4;

public class Aufgabe20 {
    /*
     * 1. boolean false, println; 
     * -> semantischer fehler, da println ein schlüsselwort ist und somit nicht als bezeichner verwendet werden darf
     * 
     * 2. char ab, uv, x y;
     * -> syntaktischer fehler, da ein bezeichner kein leerzeichen enthalten darf
     * 
     * 3. int a = 0x1, c = 1e2;
     * -> syntaktischer fehler, da ein bezeichner nicht mit einer zahl beginnen darf
     * 
     * 4. boolean b = true;
     *    System.out.println(3+b+"=3true");
     * -> semantischer fehler, da ein boolean nicht mit einer zahl addiert werden kann
     * 
     *  5. double a, b_c, d-e;
     * -> syntaktischer fehler, da ein bezeichner kein operator enthalten darf
     * 
     *  6. int mo, di, mi, do, fr, sa, so;
     * -> semantischer fehler, da die bennenung der bezeichner irreführend ist (Als Konstaten großzuschreiben)
     * 
     * 7. int a = 4;
     *    if (3 < a < 5) a = a*a;
     * -> syntaktischer fehler, da ein boolean nicht mit einer zahl verglichen werden kann
     * 
     * 8. System.out.println("10 = ", 10);
     * -> semantischer fehler, da println nur einen parameter erwartet
     */
}
